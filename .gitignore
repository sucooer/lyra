node_modules/
dist/
.npm-cache/
*.log
.DS_Store
.wrangler/
.workbuddy/

# 以下三项由 scripts/gen-meta.mjs 生成，但**是有意入库的**（2026-09-16 决定）：
# 脚本把 meta.json 本身当作解析缓存，入库后 CI 构建可命中增量缓存、跳过对全部远程音频的
# 下载与解析；否则每次构建都要从零跑一遍整个曲库，随曲库增大只会越来越慢。
# 代价：专辑封面与完整歌词也进入仓库，**加歌后要连同产物一起提交**。
# 注意：gen-meta 会清理 covers/、lyrics/ 中未被 meta.json 引用的孤儿文件，
# 所以不要把手工制作的封面/歌词放进这两个目录。
