# 复制成 .env.local 后填写（.env.local 已被 .gitignore 排除，不会进仓库）
#
# EMBY_URL   Emby 服务地址，不带结尾斜杠。注意本站是 https，
#            而 Emby 这边通常只有 http——两者不能直接互通，音频一律经
#            /api/emby/stream 中转，所以这里填 http 没问题。
# EMBY_API_KEY
#            Emby 后台 → 设置 → 高级 → API 密钥 → 新建。
#            ⚠️ 它不是「只读音乐库」的凭证，而是整台服务器的完整权限
#            （含其它媒体库与管理接口）。所以只能放在服务端：
#            本地放这个文件，线上放 CF Pages / Vercel 的环境变量。
#            绝不要写进 playlist.json / emby.json——那是公开产物。
#
# 没配也能正常构建：`pnpm emby` 会跳过，沿用仓库里已提交的那份快照。

EMBY_URL=http://192.168.1.10:8096
EMBY_API_KEY=在这里填入密钥

# LASTFM_API_KEY（可选）
#   `pnpm artists` 用它抓歌手简介。不配也能跑，只是简介改从维基百科拿。
#   免费申请：https://www.last.fm/api/account/create （拿到的是 key，不是 secret）
#   ⚠️ 同上，只放服务端：本地写在这个文件里，CI 放仓库 secret。
#   注意：Last.fm 的中文简介覆盖很有限，多数艺人只有英文条目；
#   拿不到中文时脚本会自动退回英文，再退回维基百科。

LASTFM_API_KEY=

# AI_API_KEY / AI_BASE_URL / AI_MODEL（可选）
#   配了就让模型把「每日推荐」的推荐语写成一段文艺随笔，没配就用模板算出来的一段。
#   任何一家 OpenAI 兼容接口都行（DeepSeek / 智谱 / 通义 / OpenAI…），只要这三样：
#
#     AI_API_KEY   必填。密钥。⚠️ 只放服务端：本地写在这个文件里，CI 放仓库 secret。
#     AI_BASE_URL  OpenAI 兼容根路径，例如 https://api.deepseek.com/v1
#                  不填会按 https://api.deepseek.com/v1 处理（并在日志里说明）。
#                  已经带上 /chat/completions 也认；末尾斜杠随便写。
#     AI_MODEL     模型名，例如 deepseek-chat。不填按 deepseek-chat 处理。
#
#   单次成本很低：一次请求约 1.5k 输入 token + 150 输出 token，一天一次。
#   生成时间在 GitHub Actions 上（境外网络可直连），本机 bash 出不了外网也不影响；
#   想本地看效果，可以 pnpm daily --dry 让它先打印出来。
#
#   另有三个不常用的开关：AI_TEMPERATURE（默认 1.15，越大越放飞）、
#   AI_TIMEOUT_MS（默认 60000，超时后重试一次即回落模板）。
#   强制某次不调模型：pnpm daily --no-ai

AI_API_KEY=
AI_BASE_URL=
AI_MODEL=
